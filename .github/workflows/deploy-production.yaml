name: Deploy Production

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  format:
    uses: ./.github/workflows/format.yaml

  lint:
    uses: ./.github/workflows/lint.yaml

  test-jest:
    uses: ./.github/workflows/test-jest.yaml
    secrets: inherit

  type-check:
    uses: ./.github/workflows/type-check.yaml

  test-playwright:
    needs: [format, lint, test-jest, type-check]
    uses: ./.github/workflows/test-playwright.yaml
    # Setting 'secrets: inherit' does not necessarily pass secrets from this workflow.
    # Inside 'test-playwright.yaml' -> 'environment: name: Test' are set.
    # This means secrets from test environment will be used.
    # However 'secrets: inherit' is needed.
    secrets: inherit

  deploy-production:
    needs: test-playwright
    timeout-minutes: 10
    runs-on: ubuntu-latest
    environment:
      name: Production
      url: https://car-service.842u.dev
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Get Npm Global Directory
        id: npm-global-dir
        run: echo "dir=$(npm prefix -g)" >> $GITHUB_OUTPUT

      - name: Get Latest Vercel CLI Version
        id: vercel-version
        run: echo "version=$(npm show vercel version)" >> $GITHUB_OUTPUT

      - name: Cache Npm Global Dependencies
        uses: actions/cache@v5
        id: npm-global-cache
        with:
          path: |
            ${{ steps.npm-global-dir.outputs.dir }}
          key: |
            ${{ runner.os }}-${{ github.job }}-${{ steps.vercel-version.outputs.version }}
          restore-keys: |
            ${{ runner.os }}-${{ github.job }}-${{ steps.vercel-version.outputs.version }}

      - name: Install Vercel CLI
        if: steps.npm-global-cache.outputs.cache-hit != 'true'
        run: npm install --global vercel@latest

      - name: Pull Vercel Remote Environment Variables
        run: vercel pull --yes --environment=production --token=$VERCEL_TOKEN
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        run: vercel build --prod --token=$VERCEL_TOKEN
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Project Artifacts to Vercel
        run: vercel deploy --prebuilt --prod --token=$VERCEL_TOKEN
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

      - name: Discord Notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_USERNAME: 842u_BOT
          DISCORD_AVATAR: https://avatars.githubusercontent.com/u/23544888?v=4
        uses: Ilshidur/action-discord@master
        with:
          args: |
            New PRODUCTION for {{ EVENT_PAYLOAD.repository.full_name }}
            https://car-service.842u.dev

  create-audit:
    needs: deploy-production
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: ./.github/workflows/create-audit.yaml
    secrets: inherit
